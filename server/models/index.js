const User = require('./User');
const UserScore = require('./UserScore');
const Category = require('./Category');
const Image = require('./Image');

module.exports = { User, Category, Image, UserScore };
